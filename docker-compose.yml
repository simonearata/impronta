services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: impronta
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - impronta_pg:/var/lib/postgresql/data

volumes:
  impronta_pg:
